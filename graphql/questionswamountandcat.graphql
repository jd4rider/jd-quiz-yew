query QuestionWAmountandCat($amount: Int!, $category_id: Int!, $difficulty: String!) {
  questionsByAmountAndCategoryId(amount: $amount, category_id: $category_id, difficulty: $difficulty) {
    category
    difficulty
    question
    correct_answer
    incorrect_answers
  }
}