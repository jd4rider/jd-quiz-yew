{
    "version": 2,
    "installCommand": "npm i node-fetch@2.6.7 && npm install",
    "builds": [
      {
        "src": "server/index.ts",
        "use": "@now/node"
      },
      {
        "src": "dist/**",
        "use": "@vercel/static"
      }
    ],
    "routes": [
        {
            "src": "/",
            "dest": "/server/index.ts"
        },

        {
          "src": "/graphql",
          "dest": "/server/index.ts"
        },

        {
            "src": "/(.*)",
            "dest": "/dist/$1"
        }
    ]
  }